"use strict"; //it treats all your js code as newer version 

// alert(3+3) //we are using nodejs and not browser so this won't work here

let name="Aryan" //string
let age=32 // number
let isLoggedIn=false //boolean

// number => it has range upto 2^52
// bigint => for storing huge numbers we use it
// string => it is preferred to use ""
// boolean
// null => standalone value
// undefined => iska matlab variable declare hua hai pr uski koi value nhi defined hai
// symbol => it is used in defining uniqueness 

// object

console.log(typeof undefined); // o/p will be undefined
console.log(typeof null); // o/p will be an object